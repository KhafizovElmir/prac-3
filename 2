#include <stdio.h>

int main() {

    char colors[3][10] = {"white", "red", "blue"};
    
    strcpy(colors[0], "orange");

    for (int i = 0; i < 3; i++) {
        printf("%s\n", colors[i]);
    }

    return 0;
}
